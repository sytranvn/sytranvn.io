title: Hello world! # name of the site
author: Sy Tran
email: sytranvn@gmail.com
url: "https://sytranvn.github.io" # the base hostname & protocol for your site, e.g. http://example.com
baseurl: "/" # subpath of the site, e.g. "/blog"
description: > # description of the site (multiple lines allowed)
  A (nearly) no-CSS, fast, minimalist Jekyll theme.

permalink: /:slug.html

favicon: "logo.png" # name+extension of favicon (which must be put on the root folder)
# goat_counter: "yoursitename" # put your GoatCounter name if you want to use GoatCounter analytics

remote_theme: no-style-please # if you are using GitHub Pages, change it to remote_theme: riggraz/no-style-please

theme_config:
  appearance: "auto" # can be "light", "dark" or "auto"

sass:
  style: :compressed

plugins:
  - jekyll-feed
  - jekyll-seo-tag
